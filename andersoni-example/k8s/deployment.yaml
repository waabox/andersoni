apiVersion: apps/v1
kind: Deployment
metadata:
  name: andersoni-example
  namespace: andersoni-example
spec:
  replicas: 3
  selector:
    matchLabels:
      app: andersoni-example
  template:
    metadata:
      labels:
        app: andersoni-example
    spec:
      serviceAccountName: andersoni-example
      hostAliases:
        - ip: "GATEWAY_IP"
          hostnames:
            - "infra-gateway"
      containers:
        - name: app
          image: andersoni-example:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: POSTGRES_HOST
              value: "infra-gateway"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "infra-gateway:9092"
            - name: MINIO_ENDPOINT
              value: "http://infra-gateway:9000"
            - name: K8S_NAMESPACE
              value: "andersoni-example"
            - name: POSTGRES_USER
              value: "andersoni"
            - name: POSTGRES_PASSWORD
              value: "andersoni"
            - name: MINIO_ACCESS_KEY
              value: "minioadmin"
            - name: MINIO_SECRET_KEY
              value: "minioadmin"
            - name: MINIO_BUCKET
              value: "andersoni-snapshots"
          readinessProbe:
            httpGet:
              path: /events/info
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /events/info
              port: 8080
            initialDelaySeconds: 45
            periodSeconds: 10
